# Create a .env file in this directory with the following content:

DATABASE_URL=postgresql://postgres:postgres@localhost/waffy_db

# Adjust the username, password, and database name as needed for your PostgreSQL setup
# Example with different credentials:
# DATABASE_URL=postgresql://your_username:your_password@localhost/waffy_db

PORT=8000
HOST=0.0.0.0
